{
  "name": "actions-build-and-tag",
  "version": "1.0.1",
  "private": true,
  "description": "Publish JavaScript GitHub Actions to release and floating version tags",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iShark5060/actions-build-and-tag.git"
  },
  "main": "dist/index.js",
  "scripts": {
    "validate": "node run-quality-checks.mjs",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "check-format": "oxfmt --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --target=node24 --outfile=dist/index.js --minify",
    "deps": "pnpm dlx npm-check-updates --target newest -u --reject @types/node && pnpm dlx npm-check-updates @types/node --target semver -u",
    "deps:update": "pnpm update -r --latest && pnpm update @types/node"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "semver": "^7.8.5",
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.5",
    "@types/semver": "^7.8.0",
    "esbuild": "^0.28.2",
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=26",
    "pnpm": ">=12.0.0"
  },
  "packageManager": "pnpm@12.3.4"
}
